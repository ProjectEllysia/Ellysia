# Web Framework
Flask>=3.0.0
flask-cors>=4.0.0
gunicorn>=21.2.0

# Database & ORM
SQLAlchemy>=2.0.23
psycopg2-binary
alembic>=1.13

# PDF Generation
reportlab>=4.0.7
Pillow>=11.0.0

# QR code decoding (Iris quishing detection, D1)
opencv-python-headless>=4.10.0

# Python-Nmap: Interfaz Python para Nmap
python-nmap==0.7.1

# XML Processing (for Nmap and Nikto output parsing)
lxml>=4.9.3
xmltodict==0.13.0

# YAML: plantillas de Nuclei (almacén/ingesta, roadmap Fases U4 y R). Estaba
# disponible de forma transitiva; se declara explícito ahora que hay código
# de primera parte que lo importa.
PyYAML>=6.0

# Jinja2: plantillas de correo de herald (tools/herald/templates/). Ya venía
# de forma transitiva con Flask; se declara explícito ahora que hay código de
# primera parte que la importa.
Jinja2>=3.1

# Logging & Utilities
python-dateutil==2.8.2

# Testing (optional but recommended)
pytest>=7.4.3
pytest-cov>=4.1.0
pytest-mock>=3.12.0

# Type Hints & Code Quality (development)
mypy>=1.7.1
black>=23.11.0
flake8>=6.1.0

# API Documentation & Validation
flask-smorest>=0.45.0,<0.47
marshmallow>=3.21.0,<4.0

# Security, API & Utilities
argon2-cffi>=23.1.0
cryptography>=41.0.4
flask_limiter
PyJWT
pyotp>=2.9.0
dnspython
ollama
openai>=1.0
google-generativeai>=0.8.0
requests
ddgs
psutil
python-dotenv>=1.2.2
APScheduler>=3.10
croniter>=2.0

# Task Queue & Redis
redis>=5.0.0
rq>=1.16.0

