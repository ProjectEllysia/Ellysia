# Web Framework
Flask>=3.0.0
flask-cors>=4.0.0
gunicorn>=21.2.0

# Database & ORM
SQLAlchemy>=2.0.23
psycopg2-binary
alembic>=1.13

# PDF Generation
reportlab>=4.0.7
# Pillow: no se importa directamente, pero reportlab la requiere en tiempo de
# ejecución para insertar imágenes en los PDFs (Image de platypus) — se
# declara explícito para fijar versión mínima en vez de depender de la
# transitiva de reportlab.
Pillow>=11.0.0

# Iris: dominio registrable según la Public Suffix List (empaquetada; no se
# descarga en ejecución) y tabla de confusables de Unicode para distinguir un
# dominio internacionalizado legítimo de un homógrafo.
publicsuffixlist>=1.0.2
confusable-homoglyphs>=3.3.1

# QR code decoding (Iris quishing detection, D1)
opencv-python-headless>=4.10.0

# YAML: plantillas de Nuclei (almacén/ingesta, roadmap Fases U4 y R). Estaba
# disponible de forma transitiva; se declara explícito ahora que hay código
# de primera parte que lo importa.
PyYAML>=6.0

# Jinja2: plantillas de correo de herald (tools/herald/templates/). Ya venía
# de forma transitiva con Flask; se declara explícito ahora que hay código de
# primera parte que la importa.
Jinja2>=3.1

# API Documentation & Validation
flask-smorest>=0.45.0,<0.47
marshmallow>=3.21.0,<4.0

# Security, API & Utilities
argon2-cffi>=23.1.0
cryptography>=41.0.4
flask_limiter
PyJWT
pyotp>=2.9.0
ollama
openai>=1.0
google-generativeai>=0.8.0
requests
ddgs
psutil
python-dotenv>=1.2.2
APScheduler>=3.10
croniter>=2.0

# Task Queue & Redis
redis>=5.0.0
rq>=1.16.0

# Iris: lectura de .msg de Outlook (contenedor OLE/CFB), licencia BSD
olefile>=0.46

