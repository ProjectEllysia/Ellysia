# Dependencias solo para desarrollo y tests.
# Instalar junto con las de producción:
#     pip install -r requirements.txt -r requirements-dev.txt

pytest>=7.4.3
pytest-cov>=4.1.0
pytest-mock>=3.12.0

# Genera imágenes QR en los tests de Iris (D1, quishing) -- no es una
# dependencia de producción, Iris solo *decodifica* QRs (opencv-python-headless
# en requirements.txt).
qrcode>=8.0
